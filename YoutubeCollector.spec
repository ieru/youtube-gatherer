# -*- mode: python -*-
a = Analysis(['YoutubeCollector.py'],
             pathex=['C:\\Users\\Antonio.Fernandez\\PycharmProjects\\youtube-gatherer'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='YoutubeCollector.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='YoutubeCollector')
